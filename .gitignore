# Secrets & environment (never commit)
.env
.env.*
*.env
secrets.json
credentials.json
*.pem
*.key
config.local.*
service-account*.json

# Dependencies / build output (added once there is a backend)
node_modules/
dist/
build/
.next/
.cache/
coverage/
*.log

# OS / editor clutter
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Uploaded / partner documents (belong in object storage, not git)
uploads/
storage/
*.local.html
